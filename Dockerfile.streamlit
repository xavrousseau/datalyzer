# =============================================================================
# Datalyzer — Dockerfile.streamlit (simple et pédagogique)
# Objectif : construire une image Streamlit fonctionnelle sans fioritures
# =============================================================================

# Image de base légère
FROM python:3.10-slim

# Répertoire de travail
WORKDIR /app

# Variables d’environnement (qualité de vie)
ENV PYTHONDONTWRITEBYTECODE=1 \ 
    PYTHONUNBUFFERED=1

# Dépendances Python — étape séparée pour profiter du cache Docker
COPY requirements.txt .
RUN pip install --upgrade pip && \
    pip install --no-cache-dir -r requirements.txt

# Copie du projet (code + assets + static + data structure)
COPY . /app

# Dossiers utiles (au cas où ils n’existent pas côté image)
# En local, tes volumes (./data -> /app/data, etc.) écraseront ce contenu ce qui est normal.
RUN mkdir -p /app/data/uploads /app/data/exports /app/logs

# Port Streamlit
EXPOSE 8501

# Lancement — chemin ABSOLU pour éviter toute ambiguïté
CMD ["streamlit", "run", "/app/app.py", "--server.port=8501", "--server.address=0.0.0.0"]
