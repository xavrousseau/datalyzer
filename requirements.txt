# === Core Streamlit App ===
streamlit>=1.30,<2.0
pandas>=2.0
numpy>=1.24
plotly>=5.18
requests>=2.31

# === Analyse & machine learning ===
scikit-learn>=1.3
scipy>=1.11
ydata-profiling>=4.0

# === Visualisation complémentaire ===
matplotlib>=3.7
seaborn>=0.12        # (optionnel mais souvent utilisé)

# === Lecture & export multi-format ===
openpyxl>=3.1        # Excel (.xlsx)
pyarrow>=14.0        # Parquet (.parquet)
python-snappy>=0.6   # Compression parquet Snappy
xlsxwriter>=3.1      # Optionnel, Excel stylé
# fastparquet>=2023.8 # Optionnel si tu veux l’alternative à pyarrow

# === Images & UI ===
Pillow>=10.0         # Pour chargement/redimensionnement d’images

# === Suivi, logs et ressources système ===
loguru>=0.7
psutil>=5.9          # Pour surveiller mémoire/CPU

# === Divers (utiles dans data science) ===
tqdm>=4.65           # Barres de progression (optionnel)
